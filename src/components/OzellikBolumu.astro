---
// Tek özellik bölümü — h2 + onaylı paragraf + CbsMockup görseli.
// ≥900px'te zigzag: çift sıradakilerde görsel sola geçer (CSS nth-of-type).
import CbsMockup from "./CbsMockup.astro";

const { ozellik } = Astro.props;
---

<section class="ozellik" id={ozellik.id} aria-labelledby={`${ozellik.id}-baslik`}>
  <div class="metin">
    <h2 id={`${ozellik.id}-baslik`}>{ozellik.baslik}</h2>
    <p>{ozellik.aciklama}</p>
    {
      ozellik.etiketler && (
        <ul class="etiketler" aria-label="Desteklenen TUCBS veri temaları">
          {ozellik.etiketler.map((etiket) => (
            <li>{etiket}</li>
          ))}
        </ul>
      )
    }
  </div>
  <div class="gorsel-sarici">
    <CbsMockup variant={ozellik.mockup} />
  </div>
</section>

<style>
  .ozellik {
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--bosluk-5);
    align-items: center;
    padding-block: var(--bosluk-7);
    border-top: 1px solid var(--renk-cizgi);
  }
  .ozellik:first-of-type {
    border-top: none;
  }

  h2 {
    font-weight: 600;
    font-size: clamp(1.5rem, 1.2rem + 1.4vw, 2rem);
    letter-spacing: -0.015em;
    color: var(--renk-metin);
    margin-bottom: var(--bosluk-3);
  }

  p {
    color: var(--renk-metin-soluk);
    line-height: 1.68;
    max-width: 52ch;
    text-wrap: pretty;
  }

  /* TUCBS tema çipleri */
  .etiketler {
    display: flex;
    flex-wrap: wrap;
    gap: var(--bosluk-2);
    list-style: none;
    padding: 0;
    margin-top: var(--bosluk-4);
  }
  .etiketler li {
    padding: var(--bosluk-1) var(--bosluk-3);
    background: var(--renk-vurgu-acik);
    border-radius: 999px;
    font-size: 0.8125rem;
    font-weight: 600;
    color: var(--renk-vurgu);
  }

  /* Yer tutucu görsel alanı — 1px kesikli çerçeve, Hafta 3'te CbsMockup gelecek */
  @media (min-width: 900px) {
    .ozellik {
      grid-template-columns: repeat(2, 1fr);
      gap: var(--bosluk-8);
    }
    /* zigzag: çift bölümlerde görsel (son çocuk: mockup) sola */
    .ozellik:nth-of-type(even) > :last-child {
      order: -1;
    }
  }
</style>
